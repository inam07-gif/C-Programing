
int main(){
    int arr[5]={1,2,3,4,5};
int key=40;
int index=2;
int length=sizeof(arr)/sizeof(int);
for(int i=length;i>index;i--){
    arr[i]=arr[i-1];
}
arr[index]=key;

for(int i=0;i<length;i++){
    printf("%d\n",arr[i]);
}
}

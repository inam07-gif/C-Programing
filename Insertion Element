#include<stdio.h>
#include<stdlib.h>

int elements(int p, int n ){
  p=calloc(n,sizeof(int));
  for (int i = 0; i < n; i++)
  {
    printf(" enter value of %d", i);
    scanf("%d",&p+1);
  }
  for (int i = 0; i < n; i++)
    {
        printf("enter %dth element ", i + 1);
        scanf("%d", p + i);
    }
  



}

int main(){

     int *p;
     int n;
   elements(p,n);
    
    


    return 0;
}
